import { Text, View } from "react-native";

export default function HospitalAdminSettingsScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Hospital Admin - Settings (placeholder)</Text>
    </View>
  );
}
