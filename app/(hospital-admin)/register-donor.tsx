import { Text, View } from "react-native";

export default function HospitalAdminRegisterDonorScreen() {
  return (
    <View
      style={{
        flex: 1,
        justifyContent: "center",
        alignItems: "center",
      }}
    >
      <Text>Hospital Admin - Register Donor (placeholder)</Text>
    </View>
  );
}
